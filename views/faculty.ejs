<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Faculty</title>
    <link rel="stylesheet" href="/css/faculty.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>

<body>

    <!-- 🔹 Navbar -->
    <nav class="navbar">
        <ul>
            <li><a href="/syllabus">Syllabus</a></li>
            <li><a href="/additional-info">Additional Info</a></li>
        </ul>
    </nav>

    <div class="container">
        <!-- 🔹 Department Section -->
        <section class="department">
            <h1>Department of Computer Science</h1>
            <p>The Computer Science Department offers high-quality education and cutting-edge research in various
                fields, including AI, Machine Learning, Cybersecurity, and Web Development.</p>

            <!-- 🔹 Image Slider -->
            <div class="slider">
                <button class="prev" onclick="changeSlide(-1)">&#10094;</button>
                <div class="slider-content">
                    <% departmentImages.forEach((image, index)=> { %>
                        <img src="/images/<%= image %>" class="slide"
                            style="display: <%= index === 0 ? 'block' : 'none' %>;">
                        <% }) %>
                </div>
                <button class="next" onclick="changeSlide(1)">&#10095;</button>
            </div>
        </section>

        <!-- 🔹 Faculty Section -->
        <section class="faculty">
            <h1>Faculty</h1>
            <div class="faculty-list">
                <% faculty.forEach(member=> { %>
                    <div class="faculty-card">
                        <img src="/images/<%= member.image %>" alt="Faculty Image">
                        <h2>
                            <%= member.name %>
                        </h2>
                        <p><strong>Qualification:</strong>
                            <%= member.qualification %>
                        </p>
                        <p><strong>Expertise:</strong>
                            <%= member.expertise %>
                        </p>
                    </div>
                    <% }) %>
            </div>
        </section>
    </div>

    <!-- 🔹 JavaScript for Slider -->
    <script>
        let currentSlide = 0;
        function changeSlide(direction) {
            let slides = document.querySelectorAll(".slide");
            slides[currentSlide].style.display = "none";
            currentSlide = (currentSlide + direction + slides.length) % slides.length;
            slides[currentSlide].style.display = "block";
        }
    </script>

</body>

</html>